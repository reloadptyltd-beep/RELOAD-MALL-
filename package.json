{
  "name": "reload-shopify-mall",
  "version": "1.0.0",
  "description": "Task completion app with instant withdrawals",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "tronweb": "^5.3.3"
  }
}
